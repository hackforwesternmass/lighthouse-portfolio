<nav>
  <div class="nav-wrapper">
    <div class="brand-logo">
      <%= link_to "Admin Dashboard", admin_dashboard_path, class: "bc" %>
      &nbsp;
      <%= fa_icon "angle-double-right", class: "no-padding bc", text: "Manage Classes" %>
    </div>
    <div class="dropdown-wrapper hide-on-small-only">
      <%= link_to fa_icon("ellipsis-h"), "#", class: "right ellipsis-link" %>
      <ul class="dropdown z-depth-1">
        <li><%= link_to "Create class", new_klass_path %></li>
        <li><%= link_to "Edit class", "#" %></li>
      </ul>
    </div>
  </div>
</nav>

<section id="dashboard" class="section-container">

  <% if @klasses.empty? %>
    <h3>No classes currently exist.</h3>
    <%= link_to "Create one", new_klass_path, class: "btn wave cyan lighter-2" %>
  <% else %>

<!--   <div class="row">

    <div class="input-field col s12 m9 grey-text text-darken-2">
      <%= fa_icon "search", class: "prefix" %>
      <input id="opp-search" class="search" type="text" />
      <label for="opp-search">Search</label>
    </div>

    <div class="input-field col s12 m3 grey-text text-darken-2">

      <select class="filter-classes">
        <option value="present">Current Classes</option>
        <option value="previous">Previous Classes</option>
      </select>

    </div>
  </div> -->

  <% end %>

<br>
<br>
<br>
<br>

  <% @klasses.each do |klass| %>

  <div class="class row">
    <div class="col s3 title"><%= klass.name %></div>
    <div class="col s3 instrustor"><%= klass.instructor %></div>
    <div class="col s3 time"><%= "#{klass.weekday}, #{klass.time}" %></div>
    <div class="col s3 students"><span class="bold">34</span> students</div>
  </div>
  <% end %>

</section>