<section id="goals-header">
  <%= fa_icon( "angle-down", text: "Current", right: true) %>
</section>

<section id="goals-side-panel">
  
</section>

<section id="goals">
  <div class="add-goal">
   <%= fa_icon("plus-circle", text: "Add Goal") %> 
  </div> 

  <div class="each-goal">
    <%= render @current_goals %>
  </div>
  
</section>


<%= render 'goals/new'%>